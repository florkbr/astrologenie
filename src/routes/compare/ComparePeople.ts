import { profiles } from '../../stores';

export class PeopleComparator {
    public create matrix() {

        for (let i=0; i < $profiles.length; i++) {

        }
    }

    public comparePeople(people: Array[Map<Element, number>]) {
        for (let $person of profiles) {

        }
    }
}